#include "types.h"

void Swizzle16to32(void * pSwizTexels, const void * pInTexels, const int32 width,const int32 height);
void Swizzle8to32(void * pSwizTexels, const void * pInTexels, const int32 width, const int32 height);
void Swizzle4to32(void * pSwizTexels, const void * pInTexels, const int32 width, const int32 height);
